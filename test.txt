let foo = 9 * 5;
const obj = {
    x: 100,
    y: "32",
    foo,
    complex: {
        bar: true,
    },
};


print(foo, 132)
const timeNow = time();
print(timeNow)

fn makeAdder(offset) {
    fn add(x, y) {
        x + y + offset
    }

    add
}

const adder = makeAdder(3);
print(adder(10, 5))